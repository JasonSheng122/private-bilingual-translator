# Changelog

本文件记录公开发布后用户需要知道的新功能、修复和兼容性变化。

格式参考 Keep a Changelog，但不强制完全遵循。日期使用 `YYYY-MM-DD`。

## Unreleased

### Changed

- LICENSE 版权署名改为 `JasonSheng122 and contributors`；README 新增官方发布渠道说明，提醒用户只从 GitHub Releases（以及以后公布的商店链接）安装。

## 0.2.0 - 2026-09-13

### Added

- YouTube 视频中文字幕：在 YouTube 视频页点击右下角 `幕`，扩展读取播放器自己使用的英文字幕轨道（人工字幕或自动生成字幕），按整句翻译当前时间附近的字幕，并按视频播放时间逐帧同步显示在视频底部；拖动进度条、暂停、变速和控制条自动隐藏后都保持同步。
- 字幕按整句切分：人工字幕按句末标点合并连续行，自动字幕用逐词时间按停顿断句；译文返回前先显示英文原文，播放中提前补翻下一批。
- YouTube 字幕跟随“双语翻译 / 直接翻译”设置；设置面板新增“YouTube 字幕大小”（小 / 标准 / 大 / 特大），按站点保存，改完立即生效。
- 字幕和 `幕` 按钮挂在播放器内部，普通、影院和全屏模式都能显示；字号随播放器大小自动缩放，控制条自动隐藏时字幕下移。
- 字幕显示期间隐藏播放器自带字幕；广告期间隐藏；站内切换视频时自动清理。
- 失败时显示中文提示，区分没有英文字幕、广告播放中、需要刷新页面、缺少 Key、翻译服务拒绝请求等情况。
- 可选的本机 Whisper 语音识别（进阶）：视频没有英文字幕时，运行 `npm run local-whisper` 后按住 `Alt/Option` 点击 `幕`，扩展捕获当前标签页的短音频块，只发送到本机 `127.0.0.1:8765` 识别后再翻译。
- overlay 和 `幕` 按钮提供不含字幕正文的 `data-pbt-sync-*` 同步诊断属性，便于排查同步问题。

### Changed

- 新增 `tabCapture`、`offscreen` 权限和本机 `http://127.0.0.1:8765/*` host permission，仅用于可选的本机 Whisper；未新增其他网络目的地。
- 站点设置新增 YouTube 字幕大小；popup 保存其他设置时保留原值。
- README 新增运行条件、快速上手、YouTube 字幕和本机 Whisper 使用说明；删除不需要的 `npm install` 步骤（项目没有依赖）。

## 0.1.2 - 2026-05-31

### Fixed

- 已翻译社交/内容流页面继续滚动时，右侧推荐、趋势、直播、相关内容等侧栏不再自动补翻或显示原文旁 spinner；主正文流继续自动补翻。
- 收窄 `class` / `style` 变化触发自动增量翻译的条件，避免直播卡、推荐卡、动态侧栏等纯视觉刷新反复显示原文旁 spinner。
- 修复 runtime message failure 后主按钮 loading 或原文旁待翻译 spinner 可能一直停留的问题，减少 X、Reddit 等动态页面上的无限转圈残留。

## 0.1.1 - 2026-05-30

### Added

- 补齐公开 README，说明安装、首次使用、provider 配置、权限边界、发布状态和已知限制。
- 新增 `CHANGELOG.md`，作为公开用户查看后续 bug fix 和版本变化的入口。

### Changed

- 清理公开 README 和文档中的内部发布流程措辞，让仓库首页只保留用户和开发者需要的信息。
- 翻译等待态从右侧文字状态条改为主按钮 spinner 和原文旁小型 spinner，减少滚动阅读时的干扰。
- 右侧隐藏按钮改为全局 UI 偏好；隐藏或重新显示会同步到其他已打开页面，并影响后续打开的普通网页。

### Fixed

- 已翻译页面中，Twitter/X 等社交页面鼠标悬停产生的 hover card、tooltip、popover 不再触发自动增量翻译，减少误显示等待态和付费 provider 成本风险。
- 已翻译的 Twitter/X、Reddit 等社交/内容流页面中，继续滚动出现的新正文会重新自动补翻；成本控制继续依赖用户主动翻译态、有限 debounce、并发上限和旧响应丢弃，而不是按平台禁用补翻。
- Twitter/X 等页面滚动时，互动按钮、`K/M` 统计数字和裸域名不再显示原文旁 spinner 或进入翻译请求。
- Twitter/X、Reddit 等虚拟滚动评论流中，向上回到当前页面会话已翻译过的正文时，会复用会话译文，不再重复请求 provider 或重新显示 spinner；页面框架移除双语译文 marker 时也会本地补回，避免循环等待态。
- 取消翻译后，页面会话译文缓存不再把刚恢复的 Twitter/X 左侧导航、账号区域或正文重新翻回中文。
- 扩展重载或旧 content script 状态丢失后，如果页面仍残留直接翻译中文标记，右侧按钮会识别为已翻译态；点击后刷新当前页面恢复网站原文，避免把中文残留再次翻译。
- 自然版/深度版中，provider 局部未翻译触发严格重试时，只重试未通过门槛的片段，不再把同批已成功正文重复发送给付费 provider。

## 0.1.0 - 2026-05-17

### Added

- 初始 developer preview。
- 支持免费版、自然版和深度版三种翻译质量模式。
- 支持直接翻译和双语翻译两种显示模式。
- 支持官方 Gemini API 和用户自填自定义中转站。
- 支持单次、会话和本地三种 API Key 使用方式。
- 提供右侧页面控制入口、内嵌设置页、敏感域名阻止、SPA 增量翻译和基础审计脚本。

### Security

- 默认不做遥测、分析统计、远程配置、开发者服务器或云同步。
- API Key 默认使用 `chrome.storage.session`，只有用户主动选择本地保存时才写入 `chrome.storage.local`。
- content script 不接触 API Key。
