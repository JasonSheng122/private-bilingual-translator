# EVALS.md

## 目的

本文件定义不可跳过的验收门槛。

任务不能只因为功能看起来能用就算完成。

## 门槛 1：产品门槛

通过条件：

1. 符合 README、PRIVACY、SECURITY 和已记录的产品范围。
2. 不擅自扩大版本 1 范围。
3. 尊重翻译质量模式。
4. 尊重显示模式。
5. 有可见用户结果。

## 门槛 2：安全门槛

通过条件：

1. 没有不必要权限。
2. 默认没有 all_urls。
3. 没有遥测。
4. 没有分析统计。
5. 没有远程配置。
6. 没有开发者服务器。
7. 没有 chrome.storage.sync。
8. 没有 API Key 泄露。
9. 没有原文日志。
10. 敏感域名被阻止。

## 门槛 3：密钥门槛

通过条件：

1. 单次密钥不持久化。
2. 会话密钥使用 chrome.storage.session。
3. 本地密钥必须用户主动选择。
4. API Key 不进入 content script。
5. API Key 不进入日志。
6. API Key 不进入网页 DOM；如需页面右侧配置，只能放在扩展来源 iframe 内。
7. API Key 不进入错误信息。

## 门槛 4：网络门槛

通过条件：

1. 所有网络请求都在 provider 模块内。
2. 每个 provider 有网络白名单。
3. 不静默切换供应商。
4. 没有 analytics endpoint。
5. 没有 telemetry endpoint。
6. 没有 WebSocket。
7. 没有 sendBeacon。
8. 没有 EventSource。

## 门槛 5：DOM 门槛

通过条件：

1. 可见正文可翻译。
2. input 被忽略。
3. textarea 被忽略。
4. password 被忽略。
5. contenteditable 被忽略。
6. code 和 pre 被忽略。
7. hidden 元素被忽略。
8. iframe 第一版被忽略。

## 门槛 6：显示模式门槛

直接翻译：

1. 中文替换原位置文本。
2. 当前页面会话内恢复原文有效。
3. 原文不持久化保存。
4. 页面仍可阅读。

双语翻译：

1. 原文保留。
2. 译文显示在原文下方。
3. 防止重复插入。
4. 移除译文有效。

## 门槛 7：测试门槛

通过条件：

1. 相关单元测试通过。
2. 相关集成测试通过。
3. 网络变化时必须做运行时网络审计。
4. 相关公开文档或 PR 说明已更新。
5. 模块边界变化时，相关架构或安全说明已更新。
6. AUDIT.md 清单已完成。
